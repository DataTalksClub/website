# DataTalks.Club website

This repository will contain the Django implementation of [DataTalks.Club](https://datatalks.club/).

The project is currently in the specification phase. Start with [the specification index](_docs/specs/README.md).

The public development environment will be `https://dev.dtcdev.click` once the first deployable milestone is implemented.
