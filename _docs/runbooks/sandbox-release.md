# Legacy runbook link

The word `sandbox` in this filename is the former name for the same development
deployment. Use the canonical [development release runbook](development-release.md).

That runbook is also the sole authority for the current schema-2 VERSION/source/digest identity;
this historical filename does not define a legacy release format.

This compatibility link contains no commands. Live physical names remain
unchanged until [#94](https://github.com/DataTalksClub/website/issues/94).
