"""Event-linked Q&A service boundary.

The implementation is being adapted from DataQnA at ``7704f99``.  The first
slice owns only Event relation/provisioning; participant and moderation
behavior remains in the subsequent native Q&A service slices.
"""
